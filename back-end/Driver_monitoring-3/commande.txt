macOS / Linux

python3 -m venv .venv
source .venv/bin/activate
python -m pip install --upgrade pip


Windows (PowerShell)

python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install --upgrade pip




PyTorch (CPU, passe-partout)

macOS (Intel ou Apple Silicon en CPU) :

pip install torch torchvision torchaudio


Linux/Windows (CPU)

pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu







pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121  # ou cpu: https://download.pytorch.org/whl/cpu
pip install transformers pillow
